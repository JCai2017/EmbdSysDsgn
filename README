To run the included code, run make and make test to generate the necessary .sir files and execute the code.
This model assumes the input image is always "input_small.pgm" and the test image is named "golden.pgm".

Running make clean removes .si, .sir, and .o files
